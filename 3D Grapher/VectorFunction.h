#pragma once

#include <iostream>
#include <limits>
#include "glm.hpp"

using namespace std;
using namespace glm;

vec3 PositionFunction(float t);
vec3 TangentFunction(float t);
vec3 NormalFunction(float t);
